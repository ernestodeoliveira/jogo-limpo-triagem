# docs/RELEASE_PLAN.md: plano de fechamento do v0.1 (T-19 a T-24)

Este documento é o resultado da sessão P-009 (ver `docs/prompts.md`). Refina as tarefas T-19 a T-24 de `docs/PLAN.md` §3 num backlog executável para a sessão de implementação I-013, incorporando os dois itens de documentação ainda abertos da auditoria OWASP (`docs/OWASP_LLM_AUDIT_PLAN.md` §4: O-05 e O-08) e as lacunas mapeadas em `validacao.md` (F-01 a F-05, não versionado). Nenhuma mudança de código, teste ou plano de implementação foi feita nesta sessão: apenas leitura do estado real do repo, refinamento do backlog e decisões de escopo. A execução fica para a sessão I-013 (prompt completo na seção 5).

---

## 1. Contexto e objetivo

O congelamento do v0.1 está previsto para 19/07/2026 (`docs/PRD.md` §7, marcos) e a entrega escolar para 20/07/2026. O núcleo técnico do agente (LangGraph, ferramentas, memória, validação, segurança) está completo e testado (T-01 a T-18, 378 testes offline + 15 no tier opt-in `real_llm`, CI com branch protection, auditoria OWASP com verificação delta). O que resta é inteiramente material de entrega e documentação: prompts de sistema registrados, exemplos reais de execução, um relatório de amostra, slides, e a limpeza de referências desatualizadas no README e documentos irmãos (menções a Gemini/`GOOGLE_API_KEY` que já não refletem o código, que usa endpoint local OpenAI-compatible).

Esta sessão não redecide nada já decidido (Q6 do PLAN.md, o threat model da auditoria OWASP, a redação do instrumento PGSI, os riscos residuais aceitos no backlog B-16). Ela planeja apenas como aplicar essas decisões nos artefatos de fechamento.

## 2. Estado confirmado do repo (15/07/2026)

Levantamento feito nesta sessão, contra o estado real dos arquivos (não contra o que os planos anteriores presumiam):

- **`docs/prompts.md`**: a convenção `S-0xx` para prompts de sistema está declarada na introdução do arquivo, mas nenhuma entrada `S-0xx` existe ainda. O arquivo tem só duas seções (`1. Planejamento`, `2. Implementação`). O código tem exatamente dois prompts de sistema: `CLASSIFY_SYSTEM_PROMPT` (`src/triagem/classify.py:14-25`) e `PARSE_SYSTEM_PROMPT` (`src/triagem/parsing.py:118-136`), cada um já comentado no código como candidato ao registro `S-0xx` do T-19. Não há persona nem terceiro prompt de sistema, por design (a persona fica reservada para o fork Jogo Limpo Labs).
- **`examples/`** não existe no repositório. **`reports/`** não está versionado no git (o diretório de trabalho local está vazio e git não rastreia diretórios vazios, então nem o diretório existirá num clone novo até a primeira execução gravar um relatório; o `.gitignore` já tem a exceção `!reports/sample*` pronta para versionar uma amostra quando ela existir). **`docs/slides.md`** não existe. **`docs/RELEASE_PLAN.md`** (este arquivo) não existia antes desta sessão.
- **README.md**: §4 (diagrama do fluxo) não mostra `retry_offer` nem `abort_node`. §6 (como executar) ainda descreve "Modo 1: com LLM real (Gemini)" e manda preencher `GOOGLE_API_KEY`, embora o `.env.example` já use `TRIAGE_LLM_BASE_URL`/`TRIAGE_LLM_MODEL` (F-04 de `validacao.md`). §7 e §8 (exemplos de entrada/saída) são placeholders `<!-- SUBSTITUIR ... -->` (F-02). §10/§11 (limitações/segurança) ainda não documentam a limitação aceita de relatórios em texto plano (O-08). §11/§12 ainda não documentam proveniência do modelo local (O-05). §12 (referências) usa a redação antiga "versão validada em português" para o PGSI. §13 (estrutura do repositório) cita `docs/slides.md` e `examples/` que ainda não existem, e afirma "1 exemplo versionado" em `reports/` que ainda não está lá (F-05).
- **Mesma família de inconsistência (Gemini/PGSI) em outros documentos**: `docs/PRD.md` RNF-01 (ainda cita `GOOGLE_API_KEY`) e §8 (risco "Quota/latência da API Gemini"); `docs/ARCHITECTURE.md` §9 (tabela de configuração ainda lista `GOOGLE_API_KEY`/Gemini, sem `TRIAGE_LLM_BASE_URL`/`TRIAGE_LLM_MODEL`); `docs/DECISIONS.md` D-07 (redação "versão validada em português", que o próprio `docs/PLAN.md` Anexo A já previa trocar por "versão brasileira com adaptação transcultural e validade de conteúdo (Moura et al., 2026)" nas tarefas T-21/T-23).
- **`docs/OWASP_LLM_AUDIT_PLAN.md` §4**: dos oito itens do backlog (O-01 a O-08), apenas O-05 (proveniência do modelo, versão do oMLX, autenticação do endpoint) e O-08 (limitação aceita de relatórios em texto plano, LLM02) seguem em aberto; todos os demais já foram implementados ou executados nas sessões I-006/I-012. A própria seção 4 sugere que "O-05 e O-08 (docs, podem entrar no PR do T-21)".
- **T-24 e o "padrão C-03"**: a branch protection configurada em C-03 (`docs/CI_PLAN.md`) exige o check `tests` verde via PR até para mudanças só de documentação, com `enforce_admins: true`, ou seja, não há hoje um precedente de commit direto na main sem PR. O que o próprio C-03 fez sem PR foi uma ação de configuração de repositório (a chamada de API de branch protection), não um commit de código, e mesmo assim com aprovação explícita do usuário no momento. É esse o padrão que T-24 replica: a tag `v0.1` é uma ação de repositório (não um commit em arquivo versionado), feita com aprovação explícita do usuário na hora, depois que o último PR de conteúdo já tiver passado pelo check `tests` na main. Não há tags no repositório hoje.
- **Testes e CI**: suíte offline com 378 testes (sempre verdes, sem marcador), mais 15 testes no tier opt-in `real_llm` (393 no total). O job `tests` do CI roda só a suíte offline (sem chave de API); não roda `ruff` (item C-04/C-05, adiado). O repositório está limpo na `main`, sem outros worktrees abertos além deste.
- **Critérios de aceite v0.1** (`docs/PRD.md` §6, aceites 1 a 6): (1) triagem offline ponta a ponta grava relatório; (2) resposta inválida re-pergunta, três seguidas ofertam encerrar; (3) frase de crise no meio do questionário aciona o gate imediatamente; (4) `uv run pytest` verde em máquina limpa sem `.env`; (5) README permite rodar tudo em menos de 5 minutos em máquina limpa; (6) `docs/prompts.md` contém os prompts de planejamento e os prompts de sistema finais.

## 3. Backlog refinado T-19 a T-24

| ID | Escopo exato | Arquivos | Critério de aceite | Commit sugerido | Dependências | Exige LLM real? |
|---|---|---|---|---|---|---|
| **T-19** | Nova seção "2. Prompts de sistema" em `docs/prompts.md`: `S-001` = `CLASSIFY_SYSTEM_PROMPT` verbatim (`src/triagem/classify.py:14-25`), `S-002` = `PARSE_SYSTEM_PROMPT` verbatim (`src/triagem/parsing.py:118-136`), cada um com a referência de arquivo:linha como fonte; nota explícita de que não há um terceiro prompt nem persona, por design (a persona fica para o fork Jogo Limpo Labs). A seção "Implementação" é renumerada de "2." para "3.". Opcional: atualizar os dois comentários no código de "candidate for the S-0xx prompt log" para "logged as S-00x in docs/prompts.md". | `docs/prompts.md` (+ 2 comentários opcionais em `src/`) | PRD aceite 6 (prompts de sistema finais registrados) | `docs: update prompt log with final system prompts` | nenhuma | não |
| **T-20** | Com o endpoint oMLX local no ar e aprovação explícita do usuário antes de qualquer chamada: rodar a CLI real três vezes cobrindo os três cenários que o README promete (risco baixo, moderado e um cenário de crise disparada no meio do questionário); capturar os transcritos completos em `examples/risco-baixo.md`, `examples/risco-moderado.md` e `examples/crise.md`, cada um com um cabeçalho indicando data, modo (LLM real) e nome do modelo; copiar o relatório `.md`+`.json` de uma das execuções para `reports/sample-triagem-<...>.md`/`.json` (prefixo `sample` já liberado pelo `.gitignore`). Fallback: se o endpoint não estiver disponível na hora, gravar os três cenários em modo offline (`TRIAGE_FAKE_LLM=1`), com o cabeçalho declarando explicitamente o modo offline. Lembrete operacional: o `.env` não é carregado automaticamente pelo processo; a sessão real precisa exportar `TRIAGE_LLM_BASE_URL`, `TRIAGE_LLM_MODEL` e `OPENAI_API_KEY` (token Bearer local) no shell antes de rodar a CLI, e os transcritos devem ser revisados antes do commit para garantir que nenhum token vaze neles. | `examples/risco-baixo.md`, `examples/risco-moderado.md`, `examples/crise.md`, `reports/sample-triagem-*.md`, `reports/sample-triagem-*.json` | F-03 de `validacao.md` fechado; F-02 fica parcialmente atendido (transcritos reais gravados), fechamento definitivo do F-02 só ocorre no T-21, quando os placeholders do README §7/§8 forem substituídos por esse conteúdo | `docs: add execution transcripts and sample report` | nenhuma | **sim** |
| **T-21** | Atualizar README e documentos irmãos para fechar F-04/F-05 e dobrar O-05/O-08: README §4 (diagrama ganha `retry_offer` e `abort_node`); §6 (reescrito para o endpoint local, `TRIAGE_LLM_BASE_URL`, `TRIAGE_LLM_MODEL`, `OPENAI_API_KEY` opcional, removendo toda menção a Gemini/`GOOGLE_API_KEY`); §7/§8 (trechos reais extraídos dos transcritos do T-20, substituindo os placeholders); §10/§11 (O-08: limitação aceita de relatórios `.md`/`.json` em texto plano local, mapeada à categoria LLM02); §11/§12 (O-05: proveniência do modelo local, fonte dos pesos, versão do oMLX usada, mecanismo de autenticação Bearer do endpoint, e checksum dos pesos se disponível, ver decisão (f) na seção 4); §12 (troca da redação "versão validada em português" por "versão brasileira com adaptação transcultural e validade de conteúdo (Moura et al., 2026)", mesma redação que vai para D-07); §13 (árvore do repositório atualizada para refletir `examples/`, `reports/sample*` e `docs/slides.md` reais). Fora do README, mesmo tema: `docs/PRD.md` RNF-01 e §8 (remover Gemini); `docs/ARCHITECTURE.md` §9 (tabela de configuração atualizada); `docs/DECISIONS.md` D-07 (nova redação do PGSI); `docs/OWASP_LLM_AUDIT_PLAN.md` §4 (marcar O-05 e O-08 como implementados, no mesmo formato das linhas já fechadas). | `README.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/OWASP_LLM_AUDIT_PLAN.md` | F-02 fechado de vez (placeholders §7/§8 substituídos pelo conteúdo real do T-20); F-04 e F-05 fechados; diagrama do README §4 mostra `retry_offer` e `abort_node`; README §12 e `docs/DECISIONS.md` D-07 com a nova redação do PGSI ("versão brasileira com adaptação transcultural..."); O-05 e O-08 marcados implementados; `grep -ri GOOGLE_API_KEY` e `grep -ri Gemini` no repo não retornam nada fora do histórico git | `docs: finalize readme with real examples` + `docs: document local model provenance and endpoint auth` + `docs: document accepted storage limits` (commits separados dentro do mesmo PR) | T-20 (para os trechos reais de §7/§8) | não |
| **T-22** | `docs/slides.md` no formato Marp (front-matter `marp: true`, slides separados por `---`), com exatamente 2 slides: slide 1 cobre problema, processo automatizado escolhido e proposta do agente; slide 2 cobre entrada esperada, saída esperada, as três ferramentas integradas e uma visão do fluxo LangGraph. Todo o conteúdo necessário já existe no README e no PRD; é trabalho de condensação, não de pesquisa nova. Exportável para PDF com `marp-cli` na hora da submissão ao AVA, sem versionar o PDF no repositório. | `docs/slides.md` | F-01 de `validacao.md` fechado; no máximo 2 slides; cobre os pontos exigidos pelo enunciado (problema, processo, entrada, saída, ferramenta, fluxo) | `docs: add presentation outline` | nenhuma | não |
| **T-23** | Após o merge do PR-1 (T-19+T-20+T-21+T-22): protocolo de "máquina limpa" adaptado a este ambiente, `git clone` novo do GitHub em um diretório temporário, sem copiar `.env`; rodar `uv sync`, depois `uv run pytest` (378 testes verdes, aceite 4) e a CLI offline ponta a ponta seguindo literalmente o README §6 (cobrindo os aceites 1, 2 e 3), cronometrando o tempo total desde o clone até o fim da execução (aceite 5, menos de 5 minutos); conferir visualmente que `docs/prompts.md` cobre os prompts de planejamento e os de sistema (aceite 6). Montar um checklist dos 6 aceites com o comando executado e o resultado resumido de cada um, para registrar como evidência no `Resultado` da entrada I-013 em `docs/prompts.md`. Se o clone limpo revelar qualquer problema (passo do README que não funciona, teste que falha fora do worktree de desenvolvimento etc.), corrigir antes de prosseguir. | nenhum arquivo por padrão; os que forem necessários só se o clone limpo revelar um problema real | os 6 aceites do PRD §6 executados e registrados com evidência | `chore: final acceptance review fixes` (só se houver algo a corrigir) | PR-1 mergeado | não |
| **T-24** | Depois que o PR-2 (evidência do T-23 + registro I-013) estiver mergeado e o check `tests` estiver verde na `main`: atualizar a `main` local (`git pull`), criar uma tag anotada `v0.1` (mensagem `chore(release): v0.1`) e dar `git push` da tag, ação de configuração/estado do repositório, sem PR, no mesmo padrão de C-03 (ver seção 2), com aprovação explícita do usuário no momento do push. A partir da tag, nenhum commit deve ser feito no repositório até a nota ser recebida (congelamento). | nenhum arquivo (ação de git/repo) | tag `v0.1` visível no GitHub; página do repositório confere com o que o README descreve | tag `v0.1`, mensagem `chore(release): v0.1` | PR-2 mergeado, CI verde na main | não |

## 4. Decisões (via `AskUserQuestion`, todas na opção recomendada)

- **(a) Conteúdo do T-19**: nova seção "2. Prompts de sistema" com `S-001` (`CLASSIFY_SYSTEM_PROMPT`) e `S-002` (`PARSE_SYSTEM_PROMPT`) verbatim, mais uma nota curta registrando que não há um terceiro prompt nem persona, por design. Isso ajusta a redação original de `docs/PLAN.md` ("S-001 a S-003") para "S-001/S-002": não existe um terceiro prompt de sistema no código hoje, e inventar um só para preencher a numeração seria documentação fictícia.
- **(b) Fonte dos transcritos do T-20**: LLM real, com aprovação explícita do usuário antes de qualquer chamada ao endpoint oMLX. Fallback já definido: se o endpoint não estiver disponível na hora da execução, gravar em modo offline, rotulado como tal no cabeçalho de cada transcrito.
- **(c) Formato dos slides do T-22**: `docs/slides.md` no estilo Marp (front-matter + `---` entre slides), mantendo o arquivo `.md` que o README §13 já referencia, com caminho de exportação a PDF só no momento da submissão, sem versionar o PDF.
- **(d) Granularidade dos PRs da I-013**: dois lotes mais a tag. PR-1 ("conteúdo") reúne T-19+T-20+T-21+T-22, para que o README §13 nasça consistente com `examples/`, `reports/sample*` e `docs/slides.md` já existindo no mesmo merge (evita um estado transitório em que o README cita arquivos que ainda não existem). PR-2 reúne a evidência do T-23 e o registro da própria sessão I-013 em `docs/prompts.md` (mais correções, se o clone limpo encontrar algo). T-24 (tag) não passa por PR, é ação de repositório com aprovação na hora, no padrão de C-03.
- **(e) Protocolo de "máquina limpa" do T-23** (sem outra máquina disponível neste ambiente): clone novo do GitHub em diretório temporário, sem `.env`, seguindo `uv sync` → `uv run pytest` → CLI offline conforme o README §6, cronometrado; evidência é um checklist dos 6 aceites com comando e resultado resumido, registrado no `Resultado` da entrada I-013 em `docs/prompts.md` (não um anexo separado neste plano).
- **(f) Dados do O-05** (versão do oMLX e checksum dos pesos do modelo local): coletados na própria sessão I-013, com o usuário, no momento em que o endpoint estiver no ar para o T-20 (comandos locais que só o usuário pode rodar, ex. versão do pacote oMLX instalado e `shasum`/checksum dos arquivos de peso). Se algum desses dados não estiver disponível na hora, aplicar o fallback de redação já previsto: documentar a fonte dos pesos e o mecanismo de autenticação Bearer do endpoint, e registrar o dado ausente (versão exata do oMLX ou checksum) como uma limitação de documentação aceita, sem bloquear o fechamento do T-21.

## 5. Sequência de execução da sessão I-013

1. Worktree `.worktrees/v01-closeout`, branch `docs/v01-closeout`, a partir da `main` atualizada.
2. PR-1, na ordem T-19 → T-20 (com aprovação explícita antes de qualquer chamada ao endpoint) → T-21 → T-22, com um commit por tarefa (T-21 pode ter até três commits, ver seção 3). Gates obrigatórios de code review e security review antes de abrir o PR (mesmo sendo docs-only); corrigir qualquer achado Important+ e repetir as duas revisões sobre o estado atualizado até não haver mais achados relevantes. Abrir o PR, aguardar o check `tests` verde, confirmar o merge com o usuário antes de mesclar, remover o worktree.
3. T-23 diretamente após o merge do PR-1 (fora de um worktree de desenvolvimento, num clone temporário à parte).
4. PR-2, em worktree novo: registro da entrada I-013 em `docs/prompts.md` (prompt integral + `Resultado` com o checklist do T-23) e, se houver, as correções do T-23. Mesmos gates obrigatórios, mesmo fluxo de merge com confirmação do usuário.
5. T-24 só depois do merge do PR-2 e do check `tests` verde na `main`: tag anotada `v0.1`, push da tag, com aprovação explícita do usuário no momento.
6. Atualizar a memória: marcar a entrada do prompt I-013 como executada e apontar o próximo passo pós-v0.1 (retomar B-17 a B-21 do backlog de teste, ou iniciar o fork Jogo Limpo Labs).

O prompt de implementação I-013 completo (Contexto + Papel + Tarefa + Formato) está registrado na entrada P-009 de `docs/prompts.md`, pronto para colar na sessão seguinte.

## 6. Riscos e fallbacks

| Risco | Fallback |
|---|---|
| Endpoint oMLX indisponível no momento do T-20 | Gravar os três transcritos em modo offline (`TRIAGE_FAKE_LLM=1`), com o cabeçalho de cada arquivo declarando explicitamente o modo offline |
| Versão do oMLX ou checksum dos pesos não disponíveis para o O-05 | Documentar fonte dos pesos e autenticação Bearer do endpoint; registrar o dado ausente como limitação de documentação aceita, sem bloquear o T-21 |
| Horário oficial da entrega (enunciado diz 22h no corpo e 15h no checklist) | Fora do escopo do repositório; ação do usuário confirmar com o professor antes de 20/07/2026 |
| Clone limpo do T-23 revela um passo do README que não funciona | Corrigir antes de prosseguir; a correção entra no PR-2 (`chore: final acceptance review fixes`) |
